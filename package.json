{
  "name": "d2b",
  "version": "0.0.13",
  "description": "A d3-based reusable chart library.",
  "keywords": [
    "d3",
    "d3-module",
    "chart",
    "charts",
    "pie",
    "axis",
    "line",
    "area",
    "bar",
    "stacked",
    "tooltip"
  ],
  "license": "MIT",
  "main": "build/d2b.js",
  "jsnext:main": "index",
  "homepage": "https://d2bjs.org",
  "repository": {
    "type": "git",
    "url": "https://github.com/d2bjs/d2b"
  },
  "scripts": {
    "build:dev": "npm run test && rollup -c -g d3:d3",
    "build:prod": "NODE_ENV=production rollup -c -g d3:d3",
    "build:server": "http-server -a localhost -p 8000",
    "build:watcher": "npm-watch",
    "build:watch": "npm-run-all --parallel build:watcher build:server",
    "test": "jasmine",
    "docs:prepare": "gitbook install",
    "docs:watch": "npm run docs:prepare && gitbook serve",
    "docs:build": "npm run docs:prepare && rm -rf _book && gitbook build",
    "docs:publish": "npm run docs:build && cd _book && git init && git commit --allow-empty -m 'Update docs' && git checkout master && git add . && git commit -am 'Update docs' && git push git@github.com:d2bjs/d2bjs.github.io master --force"
  },
  "dependencies": {
    "d3": "^4.5.0"
  },
  "devDependencies": {
    "babel-preset-es2015-rollup": "^3.0.0",
    "cssnano": "^3.10.0",
    "http-server": "^0.9.0",
    "jasmine-es6": "^0.4.0",
    "npm-run-all": "^4.0.1",
    "npm-watch": "^0.1.8",
    "postcss-cssnext": "^2.9.0",
    "postcss-triangle": "^1.0.1",
    "precss": "^1.4.0",
    "rollup": "0.27",
    "rollup-plugin-babel": "^2.7.1",
    "rollup-plugin-eslint": "^3.0.0",
    "rollup-plugin-postcss": "^0.2.0",
    "rollup-plugin-uglify": "^1.0.1",
    "rollup-watch": "^3.2.2",
    "tape": "4",
    "uglify-js": "2"
  },
  "watch": {
    "build:dev": {
      "patterns": [
        "src",
        "spec"
      ],
      "extensions": "js,css,scss"
    }
  }
}
